# Optional: leave blank to use the built-in template fallback chat mode.
# Never commit a real key; copy this file to `.env` for local use.
GROQ_API_KEY=

# Optional in local development. Set a long random value in Render production.
SECRET_KEY=
